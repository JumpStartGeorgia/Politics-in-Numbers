<main>
  <div class="container">
    <%= bootstrap_flash %>

    <% if content_for? :page_title %>
      <h1 class='primary-header'><%= yield(:page_title) %></h1>
    <% end %>

    <%= yield %>
  </div>
</main>
