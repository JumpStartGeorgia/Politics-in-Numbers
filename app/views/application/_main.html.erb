<main>
  <div class="container">
    <%= bootstrap_flash %>

    <% if @show_page_title == true && content_for?(:page_title) %>
      <%= underlined_header(yield(:page_title), 'h1', 'primary-header') %>
    <% end %>

    <%= yield %>
  </div>
</main>
