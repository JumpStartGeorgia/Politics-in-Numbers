# See http://www.robotstxt.org/robotstxt.html for documentation on how to use
# the robots.txt file.
#
# To ban all spiders from the entire site on a certain deploy stage, add the
# following line to config/deploy/{stage}:
#
# set :visible_to_robots, false
#
###############################################
<% if visible_to_robots %>
# <%= web_url %> is currently visible to robots
<% else %>
# <%= web_url %> is currently hidden from robots by the following two lines:
User-agent: *
Disallow: /
<% end %>
###############################################
